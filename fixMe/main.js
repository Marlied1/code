//Create a function that replaces all the dots in te specified 
//string str with dashes

//fix the bug so we can all go home early

//string will never be null

function replaceDots(str) {
    return str.replace(/\./g, '_');
}